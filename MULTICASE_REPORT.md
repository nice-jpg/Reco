> 历史实验记录：文中 xml_probe 命令已移除，当前入口见 README 的 `reco.PageSession(xml)`。

# 多用例实验结果

> 历史记录：这是上一轮直接提取方案。当前实现已改为纯结构页面表示和模型按区域读取，
> 此处命令对应的旧输出契约不再适用。请阅读 README.md 和 STRUCTURE_REPORT.md。

运行：`python3 xml_probe.py run examples --out runs`。
每个用例顺序执行 prepare、定位列表、expand、结构标注、extract、固定预期验证。

| 用例 | 商品 | 核心字段齐全 | 部分记录 | 排除的列表子项 | 固定预期比对 |
|---|---:|---:|---:|---:|---:|
| merchant1 | 4 | 3 | 1 | 2 | 20 字段通过 |
| merchant2 | 6 | 6 | 0 | 0 | 24 字段通过 |
| merchant3 | 1 | 0 | 1 | 2 | 5 字段通过 |
| merchant4 | 5 | 4 | 1 | 2 | 25 字段通过 |

核心字段指名称、月售、展示价格；优惠价可以不存在。共 16 条记录，其中 13 条齐全、3 条部分。
merchant3 的唯一商品只在屏幕底部出现名称；不会丢弃，也不会用 merchant4 的同名商品价格补齐。

## 优化依据

1. **从快照 ID 改为结构关系。** 所有用例共用一套“商品记录首个分叉为左图右文”的定位规则。
   图文关系只按相对位置检查，不使用固定屏幕坐标、商品名或硬编码节点 ID。
   名称来自信息列的标题区，月售采用明确原文锚点，价格由货币组和临近祖先内的标签关联。
2. **混合列表显式覆盖。** merchant1/3/4 有横向活动区和分组标题。所有直接子项必须解释为商品
   或附理由的排除项。未知复杂结构拒绝，避免靠过滤结果“通过测试”。
3. **缺失与数值区分。** 边缘商品的未出现字段为 null + not_observed_in_snapshot；0 元仍为 0.00。
   “新客价”使用 new_customer_price；不会误记为 arrival_price。
4. **失败可诊断。** 单页失败不阻塞其他页；重跑会清除旧产物。批量最终状态和退出码指出失败，
   不会将未知模板悄悄当作空列表成功。
5. **缩小后续模型输入。** 自动规则成功时不调用模型；失败时已有 candidates、紧凑树可供分析，
   定位成功后也有 target 子树。尚未接入自动模型回退或模板学习。

实际曾发现 coupon 横幅内部“小图标＋说明文字”也满足局部图文关系。修复是把商品边界约束在
记录的首个分叉，而不是全子树中任意一个图文组合；没有为 merchant3 添加专用分支。

## 体积

| 用例 | 原始 XML 字节 | 紧凑结构字节 | 目标子树字节 |
|---|---:|---:|---:|
| merchant1 | 158507 | 27160 | 11062 |
| merchant2 | 181368 | 32716 | 14979 |
| merchant3 | 144155 | 24021 | 2210 |
| merchant4 | 181729 | 31766 | 11563 |

这些是 UTF-8 字节，不是 token 或计费数据。完整 index 保留原属性，压缩视图未删除原始 XML。

## 验证边界

19 项测试通过：除了已有执行器测试，增加了四用例全量预期、优惠价语义、0 元、部分记录、
coupon 排除、节点重新编号、增加包装层、商品改名、重复列表歧义、未知优惠标签、同名文件
隔离、失败后旧结果清除、CLI 成功/失败退出码等验证。

四份样本都参与了规则开发，新增基准由助手读 XML 转录，不能声称是独立准确率或留出集成绩。
本轮也没有进行截图可见性验证；所有自动输出 screenshot_status=unverified。
验证器检查作用域、引用和标签，但不足以独立证明语义。下一阶段需要未参与开发的模板/滚动状态
以及人工审核的真值，尤其是同类双列广告、多货币价格区和缺少图片的商品卡片。
