[metadata]
name = reco
version = 0.1.0
description = Local Android XML region queries for agents

[options]
python_requires = >=3.9
packages =
    reco
    reco.dfx
package_dir =
    reco = .

[options.package_data]
reco.dfx = static/*
